# Uploads Directory

This directory is kept empty in version control because user-uploaded artifacts and other runtime files should not be committed. The .gitignore rules exclude all contents under `uploads/`; generate files here at runtime only.
